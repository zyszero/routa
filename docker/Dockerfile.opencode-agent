FROM node:22-alpine

RUN apk add --no-cache libc6-compat

RUN npm install -g opencode-ai

EXPOSE 4321

ENV PORT=4321
ENV HOST=0.0.0.0
ENV OPENCODE_HTTP_PORT=4321
ENV OPENCODE_HTTP_HOST=0.0.0.0

CMD ["sh", "-c", "opencode serve --host ${OPENCODE_HTTP_HOST} --port ${OPENCODE_HTTP_PORT}"]
