# Python packages for tools/ppt-template/gpt/container_tools
Pillow
pdf2image
numpy
python-pptx

# System binaries still required outside pip:
# - render_slides.py: soffice, poppler utils
# - slides_test.py: depends on render_slides.py runtime
# - ensure_raster_image.py: inkscape, gs, magick/convert, heif-convert, JxrDecApp
# - detect_font.py: fc-list, soffice
