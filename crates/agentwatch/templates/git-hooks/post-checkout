#!/usr/bin/env bash
set -euo pipefail

AGENTWATCH_BIN="${AGENTWATCH_BIN:-agentwatch}"
REPO_ROOT="$(git rev-parse --show-toplevel)"

exec "$AGENTWATCH_BIN" --repo "$REPO_ROOT" git-hook post-checkout -- "$@"
